# 🍔 美食地图 — 飞书秒搭发布前检查清单

> 应用简介：记录美食足迹 + 地图可视化 + 数据回顾的个人美食记录工具，支持多人协作共享。
>
> 验证时间：2026-08-11 | 验证环境：飞书秒搭

---

## 一、基础功能测试

| #    | 测试项                   | 预期结果                         | 验证方式                                         | 状态    |
| ---- | ------------------------ | -------------------------------- | ------------------------------------------------ | ------- |
| 1.1  | 新建记录（含标签、评分） | 记录出现在地图和列表中           | API 测试：POST → 201，返回 id                    | ✅ 通过 |
| 1.2  | 编辑记录                 | 修改内容正确保存                 | API 测试：PUT → 200，success:true                | ✅ 通过 |
| 1.3  | 删除记录                 | 记录从地图和列表中消失           | API 测试：DELETE → 200，success:true             | ✅ 通过 |
| 1.4  | 关键词搜索               | 只显示匹配的记录                 | API 测试：?keyword=火锅 → 返回 2 条              | ✅ 通过 |
| 1.5  | 按评分筛选               | 只显示评分 ≥ 该值的记录          | API 测试：?minRating=4 → 返回 9 条               | ✅ 通过 |
| 1.6  | 按标签筛选               | 只显示包含所选标签的记录         | API 测试：?tagIds=火锅 → 返回 2 条               | ✅ 通过 |
| 1.7  | 新建标签                 | 标签出现在选择器中               | API 测试：POST → 201，返回完整标签               | ✅ 通过 |
| 1.8  | 编辑/删除标签            | 变更正确生效                     | API 测试：PUT/DELETE → 200                       | ✅ 通过 |
| 1.9  | 地图标记点击弹出预览卡   | 弹出预览卡片，显示菜名/店名/评分 | E2E 验证：标记点击 → 预览卡片出现                | ✅ 通过 |
| 1.10 | 预览卡打开详情抽屉       | 打开详情抽屉，显示完整信息       | E2E 验证：详情抽屉含菜名、餐厅、评分、标签、备注 | ✅ 通过 |
| 1.11 | 地图缩放/平移            | 标记点正常显示                   | E2E 验证：缩放后标记点正常                       | ✅ 通过 |

---

## 二、协作功能测试

| #    | 测试项               | 预期结果                       | 验证方式                                   | 状态                |
| ---- | -------------------- | ------------------------------ | ------------------------------------------ | ------------------- |
| 2.1  | 设置昵称和头像       | 保存成功，各处显示正确         | 代码审查：ProfileSetupDialog 完整实现      | ⚠️ 需浏览器手动验证 |
| 2.2  | 创建协作组           | 生成 6 位分享码                | API 测试：POST → 201，返回 id + shareCode  | ✅ 通过             |
| 2.3  | 通过分享码加入       | 加入成功，显示在群组列表       | 代码审查：joinByShareCode 完整实现         | ⚠️ 需第二用户验证   |
| 2.4  | 退出协作组           | 退出成功，共享记录不再可见     | 代码审查：leaveGroup 完整实现              | ⚠️ 需第二用户验证   |
| 2.5  | 删除协作组（创建者） | 删除成功                       | API 测试：DELETE → 200，success:true       | ✅ 通过             |
| 2.6  | 非创建者删除返回 403 | 提示"只有创建者可以删除协作组" | 代码审查：ForbiddenException 校验          | ✅ 代码已实现       |
| 2.7  | "我的"视图           | 只显示自己创建的记录           | 代码审查：shareViewMode 切换逻辑完整       | ⚠️ 需浏览器手动验证 |
| 2.8  | "共享"视图           | 只显示协作组成员的记录         | 代码审查：SHARE_TABS 定义完整              | ⚠️ 需浏览器手动验证 |
| 2.9  | "全部"视图           | 显示自己的 + 协作组的记录      | 代码审查：SHARE_TABS 定义完整              | ⚠️ 需浏览器手动验证 |
| 2.10 | 新建记录选协作组     | 记录对协作组成员可见           | 代码审查：FoodEntryFormFields 含可见性选择 | ⚠️ 需浏览器手动验证 |

---

## 三、数据功能测试

| #   | 测试项            | 预期结果                                               | 验证方式                                                | 状态                |
| --- | ----------------- | ------------------------------------------------------ | ------------------------------------------------------- | ------------------- |
| 3.1 | 统计面板          | 显示总数、餐厅数、平均分、评分分布、标签分布、月度趋势 | API 测试：返回完整统计 JSON；E2E 验证：面板显示统计指标 | ✅ 通过             |
| 3.2 | 导出 JSON         | 文件内容完整，格式正确                                 | API 测试：GET /export?format=json → 返回 12 条完整数据  | ✅ 通过             |
| 3.3 | 导出 CSV          | 文件格式正确，中文不乱码                               | API 测试：GET /export?format=csv → 12 条，中文列名正确  | ✅ 通过             |
| 3.4 | 导入 JSON（追加） | 新记录正确添加，旧记录保留                             | 代码审查：importData 支持 merge 模式                    | ⚠️ 需浏览器手动验证 |
| 3.5 | 导入 JSON（替换） | 旧记录清空，新记录正确导入                             | 代码审查：importData 支持 replace 模式                  | ⚠️ 需浏览器手动验证 |
| 3.6 | 清空全部记录      | 确认后记录全删                                         | 代码审查：ClearTab 含双重确认 UI                        | ⚠️ 需浏览器手动验证 |

---

## 四、边界情况测试

| #   | 测试项               | 预期结果                                   | 验证方式                                                    | 状态          |
| --- | -------------------- | ------------------------------------------ | ----------------------------------------------------------- | ------------- |
| 4.1 | 空数据/无搜索结果    | 显示友好空状态提示                         | E2E 验证：搜索不存在关键词 → 显示「暂无符合条件的记录」     | ✅ 通过       |
| 4.2 | 协作组满员（5人）    | 提示"协作组已满（最多5人）"                | 代码审查：MAX_MEMBERS=5，事务校验                           | ✅ 代码已实现 |
| 4.3 | 无效分享码           | 提示"协作组不存在或分享码无效"             | 代码审查：NotFoundException                                 | ✅ 代码已实现 |
| 4.4 | 重复加入             | 提示"你已经是该协作组成员"                 | 代码审查：BadRequestException                               | ✅ 代码已实现 |
| 4.5 | 未设置用户信息       | 提示"请先设置用户信息"，弹出设置弹窗       | 代码审查：getUserInfoForCollab 前置校验                     | ✅ 代码已实现 |
| 4.6 | 提交失败（网络异常） | 表单不关闭，弹出错误提示，已填写内容不丢失 | 代码审查：try/catch + toast.error + rethrow 阻止 clearDraft | ✅ 代码已实现 |

---

## 五、视觉与兼容性测试

| #   | 测试项         | 预期结果                               | 验证方式                            | 状态                |
| --- | -------------- | -------------------------------------- | ----------------------------------- | ------------------- |
| 5.1 | 移动端适配     | 地图全宽 + 底部抽屉式列表              | E2E 为桌面视口，移动端待验证        | ⚠️ 需手机浏览器验证 |
| 5.2 | 桌面端不同尺寸 | 布局正常，无溢出                       | E2E 验证：桌面视口布局正常          | ✅ 通过             |
| 5.3 | 加载状态       | 数据加载时有 loading                   | 代码审查：各组件含 loading 状态处理 | ⚠️ 需浏览器手动验证 |
| 5.4 | 视觉风格       | 波普风格：亮黄背景 + 粗黑描边 + 硬阴影 | E2E 验证：整体波普风格确认          | ✅ 通过             |

---

## 六、验证汇总

### API 接口测试（13 项）

| 接口                                   | 方法                | 结果                   |
| -------------------------------------- | ------------------- | ---------------------- |
| `/api/food-entries`                    | GET                 | ✅ 200，分页正常       |
| `/api/food-entries`                    | POST                | ✅ 201，返回 id        |
| `/api/food-entries/:id`                | PUT                 | ✅ 200，success:true   |
| `/api/food-entries/:id`                | DELETE              | ✅ 200，success:true   |
| `/api/food-entries?keyword=xxx`        | GET                 | ✅ 200，关键词搜索正常 |
| `/api/food-entries?minRating=4`        | GET                 | ✅ 200，评分筛选正常   |
| `/api/food-entries?tagIds=xxx`         | GET                 | ✅ 200，标签筛选正常   |
| `/api/food-entries/statistics`         | GET                 | ✅ 200，统计完整       |
| `/api/food-entries/export?format=json` | GET                 | ✅ 200，12 条数据      |
| `/api/food-entries/export?format=csv`  | GET                 | ✅ 200，中文正确       |
| `/api/tags`                            | GET/POST/PUT/DELETE | ✅ 全部 200            |
| `/api/collab/groups`                   | GET/POST/DELETE     | ✅ 全部 200            |
| 运行时日志                             | server              | ✅ 无错误              |

### E2E 浏览器测试（5 项）

| Case                      | 结果                                  |
| ------------------------- | ------------------------------------- |
| 页面加载 - 初始渲染       | ✅ 工具栏 + 地图 + 列表均可见         |
| 列表与地图联动 - 详情抽屉 | ✅ 详情含菜名、餐厅、评分、标签、备注 |
| 视图切换 - 统计面板       | ✅ 统计指标 + 图表区域                |
| 空状态 - 搜索无结果       | ✅ 显示「暂无符合条件的记录」         |
| 视觉一致性 - 波普风格     | ✅ 亮黄背景 + 粗黑描边 + 硬阴影       |

### 待浏览器手动验证（12 项）

以下项需要在实际浏览器中操作确认（含需要第二用户参与的场景）：

| #    | 测试项            | 原因                     |
| ---- | ----------------- | ------------------------ |
| 2.1  | 设置昵称和头像    | 需验证 UI 交互流程       |
| 2.3  | 通过分享码加入    | 需第二用户验证           |
| 2.4  | 退出协作组        | 需第二用户验证           |
| 2.7  | "我的"视图切换    | 需验证视图切换后数据正确 |
| 2.8  | "共享"视图切换    | 需验证视图切换后数据正确 |
| 2.9  | "全部"视图切换    | 需验证视图切换后数据正确 |
| 2.10 | 新建记录选协作组  | 需验证协作组可见性       |
| 3.4  | 导入 JSON（追加） | 需验证文件上传 + 解析    |
| 3.5  | 导入 JSON（替换） | 需验证文件上传 + 解析    |
| 3.6  | 清空全部记录      | 需验证确认弹窗 + 删除    |
| 5.1  | 移动端适配        | 需手机浏览器验证         |
| 5.3  | 加载状态          | 需验证 loading 动画      |

---

## 七、发布建议

1. **已完成**：API 全量通过（13/13）、E2E 浏览器测试通过（5/5）、代码审查通过（边界情况 6/6）
2. **手动验证**：完成上方 12 项浏览器手动验证（重点：协作功能 + 数据导入/清空 + 移动端）
3. **找 1-2 个同事试用** → 用分享码加入协作组，体验完整协作流程
4. **在飞书秒搭后台发布** → 设置可见范围（建议先仅自己/部门可见）
5. **观察 1-2 天** → 确认无异常后再扩大可见范围

---

## 八、已知注意事项

- **字体**：已移除 Google Fonts CDN，改用系统默认字体。Bangers 装饰字体降级为系统粗体
- **评分筛选**：当前仅支持最低评分（minRating），不支持最高评分（maxRating）
- **统计时间窗口**：统计面板默认展示近 365 天数据
- **schema null 守卫**：数据库 nullable 的 userProfile 列已在服务层加防御

---

> 最后更新：2026-08-11 | 验证通过率：API 100%（13/13）· E2E 100%（5/5）· 代码审查 100%（6/6）· 手动待验证 12 项
